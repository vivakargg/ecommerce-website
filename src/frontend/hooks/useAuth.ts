// src/hooks/useAuth.ts
// Clean re-export of the useAuth hook from AuthContext.
// Import from here anywhere in the app — keeps imports consistent.

export { useAuth } from "@/frontend/context/AuthContext";
